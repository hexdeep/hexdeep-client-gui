请你加一个新功能，改一改`src/pages/vm/vm.tsx`要求，在批量操作里面添加一个新功能，就是批量更新固件，更新选中的云机所对应的主机的固件，因此，如果选中了多个在同一个主机上的云机，那么就只需要更新一次对应的重复主机即可达到效果。你需要查看其他批量操作后的点击和使用效果，然后在一定程度上模仿，同时，你需要检查已有的更新主机固件的接口，同时后端并不会提供批量更新固件的接口，你需要并行调用单个更新固件的接口。

